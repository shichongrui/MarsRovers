/* @flow */

let initialState = {
  activeTab: 'curiosity'
}

export default function navigationReducer (state: any = initialState, action: any) {
  switch (action.type) {
    default:
      return state
  }
}